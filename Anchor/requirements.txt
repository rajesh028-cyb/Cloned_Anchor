# ANCHOR API Mode - Minimal Requirements
# =======================================
# Install: pip install -r requirements_api.txt
# This is for API-only mode (no audio)

# Core (required)
regex>=2023.0.0                # Advanced regex for pattern matching

# Optional: LLM backends (uncomment one if needed)
# llama-cpp-python>=0.2.0      # Local LLM
requests>=2.31.0             # For Ollama API calls

# Optional: API Server
flask>=3.0.0                   # HTTP API server
python-dotenv>=0.20.0              # For environment variable management
# =======================================
# NOTES:
# =======================================
# The API mode uses only:
# - state_machine_v2.py (pure Python, no deps)
# - llm_v2.py (template-only mode, no deps)
# - config_v2.py (pure Python, no deps)
# - extractor.py (regex only)
# - memory.py (pure Python)
# - anchor_agent.py (orchestrator)
#
# For LLM-enhanced responses:
#   pip install llama-cpp-python
# OR run Ollama server and:
#   pip install requests
#
# For HTTP API server:
#   pip install flask
# =======================================
gunicorn>=20.0.0              # Production WSGI server for Flask (optional)